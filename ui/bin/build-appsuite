#!/bin/sh

export BASEDIR="$(dirname "$(dirname "$(readlink "$0" || echo "$0")")")"

which grunt >/dev/null || (echo "grunt binary not found in path. Consider installing it with 'npm install -g grunt-cli'" && exit 1)

[ -f local.conf ] && . ./local.conf

grunt "$@"
