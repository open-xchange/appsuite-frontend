apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appsuite-ui.fullname" . }}-test-connection"
  labels:
    {{- include "appsuite-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appsuite-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
