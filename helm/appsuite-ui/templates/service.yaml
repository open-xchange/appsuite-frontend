apiVersion: v1
kind: Service
metadata:
  name: {{ include "appsuite-ui.fullname" . }}
  labels:
    {{- include "appsuite-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default "http" }}
      protocol: TCP
      name: http
  selector:
    {{- include "appsuite-ui.selectorLabels" . | nindent 4 }}
